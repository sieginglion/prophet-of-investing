crypto:
    top: 100 # top N cryptos by market cap
    window: 90 # accumulative gross profit window size, unit: day
    range:

stock:
    top: 500 # top N stocks by market cap
    fmp_key: # https://site.financialmodelingprep.com/developer/docs/
    window: 2 # moving average window size, unit: quarter
    range:

bot_token:
